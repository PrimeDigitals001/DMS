import { CutomerSearchFilterPipe } from './cutomer-search-filter.pipe';

describe('CutomerSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CutomerSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
